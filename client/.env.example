# Browser API root. Keep /api when using the Vite development proxy.
VITE_API_URL=/api

# Vite development proxy target. This is only used by the local dev server.
VITE_DEV_API_TARGET=http://localhost:5000